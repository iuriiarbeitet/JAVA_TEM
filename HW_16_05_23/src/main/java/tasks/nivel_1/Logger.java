package tasks.nivel_1;

/**
 * Создайте класс Logger, который содержит метод log(String message), записывающий сообщение в журнал.
 *        Напишите тест, используя Mockito, чтобы убедиться, что метод log был вызван хотя бы один раз.
 */
public class Logger {

    public void log(String message){

    }
}
